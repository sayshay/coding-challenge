<%= form_for([@post, Comment.new]) do |f| %>

  <div class="form-group">
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>
  <div class="form-group">
    <%= f.submit %>
  </div>
<% end %>
